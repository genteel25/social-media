import '../../../../core/helpers/helpers.dart';

abstract class SearchCopyControllerContract {}

abstract class SearchCopyViewContract extends BaseViewContract {
  @override
  Widget build(BuildContext context);
}
