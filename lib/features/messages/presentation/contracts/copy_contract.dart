import '../../../../core/helpers/helpers.dart';

abstract class MessageCopyControllerContract {}

abstract class MessageCopyViewContract extends BaseViewContract {
  @override
  Widget build(BuildContext context);
}
